global:
  resolve_timeout: 1m

route:
  receiver: aiops-platform
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 10m

receivers:
  - name: aiops-platform
    webhook_configs:
      - url: http://host.docker.internal:8000/alerts/webhook
        send_resolved: true

